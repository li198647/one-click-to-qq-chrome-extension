# ==========================================================
# 敏感文件：绝对不要提交（含 AppSecret / openid）
# ==========================================================
bridge/config.json
bridge/state.json
bridge/log/

# 宿主清单（1.0.4）：由桥启动时自己生成，里面写的是**本机绝对路径**，
# 换个目录/换台机器就失效 —— 所以不进仓库，运行时自然会重建一份。
bridge/com.mumu.qq_bridge.json

# ==========================================================
# 运行产生的中间文件
# ==========================================================
bridge/*.txt
bridge/__pycache__/
tools/*.txt
tools/*.png
tools/*.log
# 自检脚本的沙箱目录（test_native_host.py / test_tray.py 会自己清，
# 但万一某次跑崩了留下残留，别让它进仓库）
tools/_*/
# ==========================================================
# 本地开发产物：打桩版的弹窗预览页（由 tools/make-preview.js 生成）
# ==========================================================
tools/preview-*.html
*.pyc

# ==========================================================
# 发版打包产物（由 tools/make-release-package.ps1 生成）
# 不进仓库 —— 它们是要当作 GitHub Release 附件上传的，源文件都在仓库里
# ==========================================================
dist/

# ==========================================================
# 本地工作数据（含个人记忆，不属于代码）
# ==========================================================
.workbuddy/

# ==========================================================
# 系统 / 编辑器
# ==========================================================
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
